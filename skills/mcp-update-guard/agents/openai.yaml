interface:
  display_name: "MCP Update Guard"
  short_description: "Safely update Oracle GPT and shared automation."
  default_prompt: "Update the requested shared automation while preserving customizations and keep Oracle as the only GPT browser backend."
policy:
  allow_implicit_invocation: true
